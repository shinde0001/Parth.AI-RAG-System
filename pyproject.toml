[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]
ignore = ["B008", "E501"]

[tool.pytest.ini_options]
minversion = "6.0"
addopts = "-ra -q -p no:launch_testing"
testpaths = [
    "tests",
]
